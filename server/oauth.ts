// This file is no longer needed as we're using Firebase for Google authentication
// OAuth functionality has been moved to Firebase Authentication

export function initializeOAuth() {
  // OAuth initialization is now handled by Firebase on the client side
  // This function is kept for backward compatibility but does nothing
  console.log("OAuth is now handled by Firebase Authentication");
}